# LeRobot and dependencies
lerobot[feetech] @ git+https://github.com/huggingface/lerobot.git
torch>=2.0.0
torchvision>=0.15.0
huggingface_hub>=0.20.0
datasets>=2.14.0
transformers>=4.40.0
opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0
pyserial>=3.5
Pillow>=10.0.0
einops>=0.7.0

# Experiment tracking
wandb>=0.16.0

# TensorRT acceleration (optional — install separately from NVIDIA)
# tensorrt>=8.6.0        # pip install tensorrt  (requires CUDA 11.8+)
# onnx>=1.14.0           # pip install onnx
# onnxruntime-gpu>=1.16  # pip install onnxruntime-gpu  (ONNX sanity check)
